---
description: 文档驱动开发工作流（PRD → Todo/Questions → 实现 → 改动总结 → 用户验收）
alwaysApply: true
---

# 文档驱动工作流

实现任何功能时，遵循以下顺序，不要直接从聊天里的口头需求跳到写代码。

## 步骤

1. **读 PRD**：先看 `docs/prd/<功能>.md`。没有就提示用户先写（或协助起草）。
2. **拆任务**：在 `todos/<功能>.todo.md` 列实现任务，用 `- [ ]` / `- [x]` 跟踪。
3. **记歧义**：发现需求缺失、矛盾、有多种解释时，**禁止自行猜测实现**。写到 `questions/<功能>.q.md`，等用户回答后再动。
4. **实现**：按 todo 逐项做，遵守 `.cursor/rules/flutter-getx.mdc` 的代码约定。
   - 开始执行前先编号：读 `docs/task-log.md` 取最后批次 +1，回复里声明「🔢 第 N 次任务 / #NNN」，并在日志追加一行；结束更新状态。
5. **改动总结**：完成后直接在聊天里用 md 总结**改了哪些文件 / 做了什么 / 怎么验**（不再写 `specs/` 验收文件）。
6. **验证交用户**：设备验证默认由用户亲自验收；AI 不必自己截图逐条核对。需要时可 `fvm flutter analyze` 自查；用户要求时才驱动设备（先 `fvm flutter devices` 选设备，再 `fvm flutter run`）。

## 命名约定

同一功能用同一 slug（如 `login-register`）贯穿 prd / todo / questions 三个文件，方便对应。

## 歧义判断标准（满足任一即写入 questions）

- PRD 没写但实现必须知道的细节（如密码规则、错误文案）
- 两处需求互相矛盾
- 一句话能有多种实现方式且影响明显
- 涉及外部依赖（验证码、后端字段）你无法独自验证
